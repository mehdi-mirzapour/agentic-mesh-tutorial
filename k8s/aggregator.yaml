apiVersion: apps/v1
kind: Deployment
metadata:
  name: aggregator
  namespace: agentic-mesh
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aggregator
  template:
    metadata:
      labels:
        app: aggregator
    spec:
      containers:
        - name: aggregator
          image: your-registry/agentic-mesh:latest
          args: ["aggregator"]
          envFrom:
            - configMapRef:
                name: mesh-config
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
